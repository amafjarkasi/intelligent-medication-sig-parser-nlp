{
  "name": "medical-data-normalizer-sandbox",
  "version": "1.0.0",
  "description": "Sandbox environment for medical data normalization with ML fallback",
  "main": "sandbox.js",
  "type": "module",
  "scripts": {
    "sandbox": "node sandbox.js",
    "sandbox:safe": "edge --safe sandbox.js"
  },
  "dependencies": {
    "@xenova/transformers": "^2.17.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
